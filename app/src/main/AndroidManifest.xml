<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fssexample2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FSSExample2">
        <activity android:name=".Send"></activity>
        <activity android:name=".TopUpConfirm" />
        <activity android:name=".TopUpPin" />
        <activity android:name=".TopUp" />
        <activity android:name=".User" />
        <activity android:name=".SignInInternet" />
        <activity android:name=".SignInMpin" />
        <activity android:name=".ConfirmMpin" />
        <activity
            android:name=".Dashboard"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" />
        <activity android:name=".SetMpin" />
        <activity android:name=".Register" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>